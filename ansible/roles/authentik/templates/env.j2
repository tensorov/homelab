# {{ ansible_managed }}

# Authentik .env (auto-generated by Ansible)
POSTGRES_USER={{ authentik_postgres_user | default('authentik') }}
POSTGRES_PASSWORD={{ vault_authentik_postgres_password }}
POSTGRES_DB={{ authentik_postgres_db | default('authentik') }}
AUTHENTIK_SECRET_KEY={{ vault_authentik_secret_key }}
AUTHENTIK_BOOTSTRAP_PASSWORD={{ vault_authentik_bootstrap_password }}
AUTHENTIK_BOOTSTRAP_TOKEN={{ vault_authentik_bootstrap_token }}
AUTHENTIK_EMAIL__HOST={{ authentik_email_host | default('smtp.zeitoven.ru') }}
AUTHENTIK_EMAIL__PORT={{ authentik_email_port | default('587') }}
AUTHENTIK_EMAIL__USERNAME={{ authentik_email_username | default('') }}
AUTHENTIK_EMAIL__PASSWORD={{ vault_authentik_email_password | default('') }}
AUTHENTIK_EMAIL__USE_TLS={{ authentik_email_use_tls | default('true') }}
AUTHENTIK_EMAIL__FROM={{ authentik_email_from | default('idp@zeitoven.ru') }}
