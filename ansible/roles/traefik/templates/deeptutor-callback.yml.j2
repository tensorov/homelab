# {{ ansible_managed }}

http:
  routers:
    deeptutor-callback:
      rule: "Host(`{{ deeptutor_domain | default('xn--pdagoge-5wa.' + domain) }}`) && PathPrefix(`/outpost.goauthentik.io`)"
      entryPoints:
        - web-secure
      service: authentik-server-svc
      tls:
        certResolver: default
      priority: 300
