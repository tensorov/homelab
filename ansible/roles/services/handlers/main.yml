---
- name: Restart services stack
  community.docker.docker_compose_v2:
    project_src: "/opt/services/{{ item }}"
    state: restarted
  loop:
    - searxng
    - mirotalk
    - jupyter
    - smtp
    - opencode-webhook
    - opencode-web
