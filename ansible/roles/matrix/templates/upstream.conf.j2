# Matrix upstream (nginx) — generated by Ansible
# Guides Synapse federation traffic

upstream matrix-upstream {
    server synapse:8008;
}

upstream sliding-sync-upstream {
    server sliding-sync-proxy:8070;
}

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name {{ matrix_homeserver_name | default('matrix.' + domain) }};
#
#     ssl_certificate     /etc/letsencrypt/live/{{ matrix_homeserver_name | default('matrix.' + domain) }}/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/{{ matrix_homeserver_name | default('matrix.' + domain) }}/privkey.pem;
#
#     location / {
#         proxy_pass http://matrix-upstream;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
